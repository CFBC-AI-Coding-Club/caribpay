# Repo root — read by docker-compose only.
# Copy to .env if you need to change a published port.

# Host port for the Redis container. Change this if something already owns 6379
# on your machine; then set REDIS_URL in apps/api/.env to the same port.
REDIS_PORT=6379
